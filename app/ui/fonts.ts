import {Acme, Montserrat, Lusitana} from 'next/font/google'

export const acme = Acme({subsets: ['latin'], weight: ['400']})

export const lusitana = Lusitana({subsets: ['latin'], weight: ['400']})

export const montserrat = Montserrat({
    subsets: ['latin'], 
    weight: ['400']})